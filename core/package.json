{
  "name": "@fastify-app-tony/core",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./dist/index.mjs"
  },
  "main": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "scripts": {
    "build": "nanobundle build",
    "clean": "nanobundle clean && yarn rimraf ./node_modules",
    "dev": "concurrently \"yarn:dev:*\"",
    "dev:build": "watchlist src --eager -- yarn build",
    "dev:test": "vitest ./src",
    "test": "vitest run ./src"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "nanobundle": "^1.5.0",
    "rimraf": "^4.4.0",
    "vite": "^4.1.4",
    "vitest": "^0.29.2",
    "watchlist": "^0.3.1"
  },
  "ultra": {
    "concurrent": [
      "clean"
    ]
  }
}
